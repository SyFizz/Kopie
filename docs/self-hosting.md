# Documentation auto-hébergement Kopie

> ⚠️ **Placeholder** — sera rédigé en Story 6.6 (Documentation auto-hébergement).

Contenu prévu :

- Prérequis serveur (CPU, RAM, disque, OS supportés)
- Installation Docker / Docker Compose
- Configuration des variables `.env` (production)
- Configuration DNS (`prof.kopie.cc`, `eleve.kopie.cc`, `api.kopie.cc`)
- Procédure de mise à jour (pull image, migrations, restart)
- Sauvegarde / restauration PostgreSQL
- Monitoring et logs structlog
- Procédure de purge des données élève (FR-41)
